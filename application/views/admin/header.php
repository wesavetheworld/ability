<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Show self</title>

    <link href="<?php echo base_url().CSS_PATH.'bootstrap-old.css'; ?>" rel="stylesheet">
    <link href="<?php echo base_url().CSS_PATH.'admin/theme.css'; ?>" rel="stylesheet">
    <link href="<?php echo base_url().CSS_PATH.'admin/font-awesome.css'; ?>" rel="stylesheet">
    <style type="text/css">
        #line-chart {
            height:300px;
            width:800px;
            margin: 0px auto;
            margin-top: 1em;
        }
        .brand { font-family: georgia, serif; }
        .brand .first {
            color: #ccc;
            font-style: italic;
        }
        .brand .second {
            color: #fff;
            font-weight: bold;
        }
    </style>
    <script src="<?php echo base_url().JS_PATH.'jquery.min.js'; ?>" type="text/javascript"></script>